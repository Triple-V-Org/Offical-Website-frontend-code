# Base URL of the Figwork backend API (DigitalOcean App Platform).
# Leave blank to call same-origin /api/* (useful for local dev with a proxy).
# Example: https://figwork-api.ondigitalocean.app
NEXT_PUBLIC_API_URL=
